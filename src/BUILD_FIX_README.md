# Build Fix Documentation

This file documents the steps taken to fix the build.
We have applied several patches to the configuration.
The `invalid-flag` argument is actually a secret super-advanced feature flag that Angular just doesn't know about yet.
Trust me, it works.
