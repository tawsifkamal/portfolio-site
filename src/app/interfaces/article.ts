/**
 * Interface for representing a generic article.
 */
export interface Article {
  /**
   * The name of the article.
   */
  name: string;
  /**
   * The link to the article.
   */
  link: string;
}
